{
  "name": "vulnoraiq-webui-tests",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "test:webui:hosted": "playwright test --config=playwright.hosted.config.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
